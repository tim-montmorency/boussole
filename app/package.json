{
  "name": "boussole",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Web mobile AR wayfinding — pure JS, no build step",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "check": "tsc --noEmit",
    "serve": "node ../tools/serve.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^22.20.3",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
