[package]
name = "boussole-forge"
version = "0.1.0"
edition = "2021"
description = "Venue bundle authoring CLI for boussole (validate, hash, qr-sheet)"
license = "AGPL-3.0-or-later"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
qrcode = "0.14"
image = "0.25.10"
